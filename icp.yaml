canisters:
  - name: frontend
    recipe:
      type: "@dfinity/asset-canister@v2.1.0"
      configuration:
        dir: dist
        version: asset-canister-404-support
        build:
          - npm install
          - npm run build
