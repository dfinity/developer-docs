networks:
  - name: local
    mode: managed
    gateway:
      port: 0

canisters:
  - name: frontend
    recipe:
      type: "@dfinity/static-site@v0.3.1"
      configuration:
        build:
          - npm install
          - npm run build
        dir: dist
